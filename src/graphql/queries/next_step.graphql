query GetNextStep($session: ID!, $question: ID!) {
  nextStep(session: $session, question: $question)
}

query EvaluateStep($session: ID!, $question: ID!) {
  evaluateStep(session: $session, question: $question)
}
